import Box from "@mui/material/Box";

function Header() {
  return (
    <Box
      component="header"
      id="header"
      sx={{ position: "fixed", display: "none" }}
    >
    </Box>
  );
}

export default Header;

import Box from "@mui/material/Box";

function Collections() {
  return (
    <Box
      component="section"
      id="collections"
      sx={{ height: "100vh", background: "#060608", color: "#ffffff" }}
    >
      Collections
    </Box>
  );
}

export default Collections;

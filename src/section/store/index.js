import Box from "@mui/material/Box";

function Store() {
  return (
    <Box
      component="section"
      id="store"
      sx={{ height: "100vh", background: "#ffd6e2" }}
    >
      Store
    </Box>
  );
}

export default Store;

import Box from "@mui/material/Box";

function Staking() {
  return (
    <Box
      component="section"
      id="staking"
      sx={{ height: "100vh", background: "#0a216b" }}
    >
      Staking
    </Box>
  );
}

export default Staking;
